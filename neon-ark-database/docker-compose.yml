services:
  postgres-db:
    container_name: project3
    image: postgres:16
    restart: always

    environment:
      POSTGRES_USER: jessa
      POSTGRES_PASSWORD: password
      POSTGRES_DB: project3

    ports:
      - "5437:5432"

    volumes:
        - ./:/docker-entrypoint-initdb.d/